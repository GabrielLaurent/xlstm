data:
  train_size: 1000
  val_size: 200
  input_size: 10
  sequence_length: 20
model:
  hidden_size: 20
  num_layers: 2
training:
  learning_rate: 0.001
  num_epochs: 10
  batch_size: 32
  use_cuda: true